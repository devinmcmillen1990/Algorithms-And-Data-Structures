#include <vector>

class Solution {
public:
    // Replace with the function signature required by the problem.
    std::vector<int> solve(const std::vector<int>& input) {
        return input;
    }
};
